version: 1.0

runtime: python311

build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  command: gunicorn tools.server:app --bind 0.0.0.0:8080 --workers 2 --timeout 120
  network:
    port: 8080
    env: APP_PORT
  env:
    - name: PYTHONUNBUFFERED
      value: "1"
